# Notes de conception

- `core/` contient le pipeline de transformation CBC → Excel.
- `data/` contient les CSV d’entrée et les sorties `.xlsx` (non inclus dans les artefacts).
- Les catégories sont lues depuis un CSV dédié pour la classification automatique.
